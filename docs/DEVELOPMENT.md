# 开发与维护文档

> 面向后续开发者/维护者。读完本文你应该能：跑起来、测起来、知道每个文件干什么、知道怎么加一个训练动作/文章/游戏、知道哪些内容不能随便改。

## 1. 项目是什么

「脑梗康复助手」：面向**脑梗（缺血性脑卒中）恢复期患者及家属**的家庭康复辅助工具。核心场景是出院回家后的日常：每天练什么怎么练、按时吃药、测血压做记录、认识复发信号。

目标用户是老年患者和中年家属，使用环境不可控（老旧安卓机、微信内置浏览器、被儿女远程指导）。这决定了后面所有技术取舍。

## 2. 技术决策记录（为什么是现在这个样子）

| 决策 | 理由 | 推翻条件 |
|---|---|---|
| 纯静态 HTML/CSS/JS，零依赖、零构建 | 双击 `index.html` 就能用；任何静态托管都能部署；十年后依然能跑，不存在依赖腐烂 | 需要多端同步/远程查看时再上后端 |
| 普通 `<script>` 标签 + 全局对象，**不用 ES Modules** | ES Modules 在 `file://` 协议下被浏览器 CORS 策略阻止，会导致"双击打开白屏"。**因此脚本加载顺序重要**（见 §4） | 放弃 file:// 直开支持时 |
| 数据只存 localStorage，无账号无上传 | 避免服务端收集与泄露面，老人无需注册登录；但本地数据和下载备份仍是明文健康信息，受设备、浏览器账户和备份保管方式保护 | 用户明确需要多设备同步时（届时优先考虑导出/导入文件方案，其次才是服务端） |
| 手写 canvas 图表 / 手写小游戏 | 引入 chart.js 等会破坏"零依赖"，且需求只是简单折线 | 图表需求复杂化时 |
| innerHTML 模板字符串渲染 | 无构建约束下最直接的方案；配合转义纪律（见 §6 安全） | 交互复杂度显著上升时考虑迁移框架 |
| 适老化：18px 基准/三档字号/≥48px 触控 | 参考工信部适老化通用设计规范的思路 | 不要推翻 |

UI 与 UX 的组件、状态和验收标准见 [DESIGN-SYSTEM.md](DESIGN-SYSTEM.md)；域名切换步骤见 [DOMAIN-MIGRATION.md](DOMAIN-MIGRATION.md)。

## 3. 目录结构

```
stroke-rehab-assistant/
├── index.html            # 应用外壳：顶栏、视图容器、底部导航、modal/toast 挂载点
├── _headers              # Cloudflare Pages 生产响应头（CSP/HSTS/防嵌入等）
├── manifest.json         # PWA manifest（支持"添加到主屏幕"）
├── icon.svg              # 应用图标
├── css/style.css         # 全部样式。顶部 :root 定义设计变量（颜色/圆角/导航高度）
├── js/
│   ├── data-exercises.js # 【内容】训练动作库 EXERCISES + 分类 EX_CATS + 阶段 STAGES + 每日推荐 DAILY_PLAN
│   ├── data-articles.js  # 【内容】科普文章 ARTICLES + 紧急识别 BEFAST
│   ├── storage.js        # 【逻辑】Store：localStorage 数据层，唯一的数据读写入口
│   ├── charts.js         # 【逻辑】Charts.line()：零依赖 canvas 折线图
│   ├── games.js          # 【逻辑】Games：4 个认知训练小游戏
│   ├── figures.js        # 【逻辑】FIGURES：训练动作双帧简笔示意图
│   ├── speech.js         # 【逻辑】Speech：Web Speech API 朗读封装
│   └── app.js            # 【逻辑】App：视图渲染、训练引导器、弹窗、导航、初始化
├── test/
│   ├── storage.test.js   # 数据层单元测试（node 直接跑）
│   ├── encryption.test.js # 可选加密备份单元测试（node 直接跑）
│   ├── contracts.test.js  # 跨模块静态契约检查（node 直接跑）
│   ├── figures.test.js    # 简笔画几何校验（node 直接跑）
│   ├── speech.test.js     # 朗读层：归一化规则全表 + 句间停顿（node 直接跑）
│   ├── overlay.test.js    # 真实 Chromium 浮层/返回/急救/布局回归
│   ├── settings.test.js   # 真实 Chromium 即时生效偏好落盘与回显
│   ├── backup-stress.test.js # 接近上限的大备份压力回归
│   ├── preview-figures.js # 简笔画截图（目视用，非断言）
│   └── smoke.sh          # 无头浏览器冒烟测试（5 个页面/file:// + 资源可达）
├── docs/
│   ├── HANDOVER.md       # 交接文档：接手状态、已验证/未验证、环境备忘、红线（新接手先读）
│   ├── DEVELOPMENT.md    # 本文档
│   └── RESEARCH.md       # 产品调研结论（需求痛点、循证依据、竞品、设计规范）
├── AGENTS.md             # 【硬约定唯一真源】所有 AI 工具都读这份；含验证命令与「改完之后」纪律
├── CLAUDE.md             # 仅一页指针，指向 AGENTS.md（**不要在这里另写一份**，曾分叉出真错误）
└── README.md             # 面向使用者的说明
```

**内容与逻辑分离**是本项目最重要的结构约定：改训练动作、改文章**只动 `data-*.js`**，不需要碰任何逻辑代码。

## 4. 脚本加载顺序（不能乱）

`index.html` 底部按此顺序加载，后者依赖前者定义的全局名：

```
data-exercises.js → data-articles.js → storage.js → charts.js → games.js → figures.js → speech.js → app.js
```

全局名清单：`EXERCISES` `EX_CATS` `STAGES` `DAILY_PLAN` `ARTICLES` `BEFAST` `Store` `Charts` `Games` `FIG` `POSES` `FIGURES` `Speech` `App`。新增文件时在 app.js 之前插入，并在此处记录（同时记得加进 `test/smoke.sh` 的静态资源清单；`deploy.sh` 整目录拷贝 `js/`，无需改）。

## 5. 数据模型（localStorage）

单一 key：`strokeRehab.v1`（版本号在 key 名里；不兼容的 schema 变更时新建 `strokeRehab.v2` 并写迁移代码，见下）。完整 schema：

```js
{
  profile: {
    name: '',            // 称呼，显示在问候语里
    strokeDate: '',      // 发病日期 YYYY-MM-DD，用于计算"康复第N天"（发病日=第1天）
    stage: 'sitting',    // 康复阶段 bed|sitting|standing|walking，决定推荐训练
    font: 'normal',      // 字号 normal|large|xlarge
    speechRate: 'slow',  // 朗读语速 slow|mid|fast（默认慢；枚举值在 load() 里消毒）
    height: '',          // cm，选填，用于 BMI
    targets: {           // 个人目标值（遵医嘱、可调），数字在 load() 里消毒
      bpSys: 140, bpDia: 90, gluFast: 7.0, gluPost: 10.0,
    },
  },
  meds: [                // 药物清单
    // from = 开始吃的日期（新登记默认当天，避免把登记之前的日子算成漏服）
    // to   = 最后一次服药的日期（含当天），'' 表示还在吃
    // 停药只写 to、**不删记录**：删了复诊查不到吃过什么，留着不管又会天天算漏服。
    // 所有服药计数都走 Store.medsOn(date)，所以停药/加药不影响历史日期的分母。
    { id, name, dose, times: ['08:00','20:00'], note, from: 'YYYY-MM-DD', to: '', previousCourseId: '' }
  ],
  medLog: {              // 服药核对记录
    'YYYY-MM-DD': { '<medId>@<HH:MM>': true }
  },
  vitals: {
    bp:      [{ id, date, time, sys, dia, pulse }],   // 血压
    glucose: [{ id, date, time, gtype, value }],      // 血糖，gtype: 空腹|餐后2小时|随机
    weight:  [{ id, date, value }],                   // 体重 kg
  },
  exerciseLog: { 'YYYY-MM-DD': ['exId', ...] },       // 训练打卡（同日去重）
  gameLog:     { 'YYYY-MM-DD': [{ game, score, detail, time }] },  // 游戏成绩
}
```

约定：

- **所有读写必须走 `Store` 的方法**，不要在视图代码里直接摸 `localStorage`。
- `Store.load()` 通过 `normalizeState()` 对缺字段与非法嵌套值做深度规范化；读取失败时用空值维持页面可打开，同时禁止写入并保留原件。`storageStatus()` 向界面提供持续错误，`originalData()` 仅提供读取失败的原始文本供人工排查；该文本不是已校验备份。
- **隐私边界**：`localStorage` 与普通 JSON 备份都是明文。不做“应用内加密”，因为密钥若同存在浏览器里不能抵御设备或同源脚本失陷，而要求患者每次输入口令会增加锁死数据的风险。v0.2.21 仅为需要放网盘/共享电脑的备份提供主动选择的口令加密（PBKDF2-SHA-256 600000 次 + AES-256-GCM，随机 16-byte salt / 12-byte IV，口令不保存）；普通备份仍是默认主操作，旧明文备份保持兼容。忘记口令无法恢复，弱口令仍可能被猜中，界面必须同时说明这两个边界。
- 日期一律 `YYYY-MM-DD` 本地时区字符串（`Store.today()` / `Store.addDays()`），不要用 `Date.toISOString()`（会有 UTC 偏移导致的跨天 bug）。
- 未来做 schema 迁移：在 `load()` 里检测旧 key 存在且新 key 不存在 → 转换 → 写新 key（保留旧 key 一段时间以便回滚）。

## 6. 各模块要点

### app.js（主逻辑，约 2100 行；顶部有「模块地图」目录，各视图区标题带 `【区】` 前缀可搜索跳转）

- **视图注册表** `RENDERERS = { today, train, records, meds, learn }`，`App.go(view)` 切换。支持 URL 参数 `?view=xxx` 直达（冒烟测试和深度链接依赖此特性）。
- **渲染模式**：每个 `renderXxx()` 整段重建 `#view` 的 innerHTML，然后绑定事件。数据变更后调 `render(currentView)` 重渲染。没有虚拟 DOM，没有局部更新——数据量小，整页重渲染足够快，别过早优化。
- **少算数原则（v0.2.12，改 UI 时必须遵守）**：卒中后计算障碍很常见，界面里**不要出现需要患者心算或理解比率的东西**。已有的三个工具函数，加新指标/新进度时直接复用：
  - `leftText(done, total, unit)` → "还差 3 次"（不写 `2/5`、不写百分比）；
  - `dotsHTML(done, total)` / `repTrackHTML(done, total)` → 圆点或进度条（>12 个自动改进度条，否则糊成一片）；
  - `deltaLineHTML(kind)`、`chartSummaryHTML(kind, list)`、`plainDuration(sec)` → 差值、图表小结、"大约 5 分钟"都由应用算好再说。
  - 结论在前、数字在后（如 BMI 写成"体重适中（BMI 21.6）"）。给医生看的精确数字（百分比、导出报告）保留，但标注"给医生看的"并降为次要信息。
- **认同患者的措辞（v0.2.12）**：不打分、不排名、不按成绩分档给评语；连续天数断了先肯定历史（`Store.bestStreak()`）再说"做一个动作就重新开始"；做不到/漏做一律按"很常见、不是您的问题"处理并给出可执行的办法，不用威慑或指责。医学事实与安全警示不得因此删弱（见 §8）。
- **浮层与返回键（v0.2.22，加浮层时必须照做）**：安卓实体返回键默认会直接退出应用，对老人是很糟的体验。每开一个浮层就 `overlayPush()` 压一个历史条目；实体返回键或屏幕上的 ✕ / Esc 都只发起 `history.back()`，统一由 `popstate` 调 `closeTopOverlayDOM()` 关最上层，保证 DOM 与历史状态同步。主动关闭先置 `dismissPending` 并禁用控件，避免回退完成前快速双击重复保存。父任务打开帮助或独立任务时直接 `openModal()` 叠子层，保留父层 DOM；同一任务切换步骤才用 `close.replace(openNext)` 原位替换，既不 back 也不再次 push；`openTrainer` 换动作同样复用已有条目（`hadTrainer` 判断）。没有浮层时不拦返回键。相关回归必须跑 `node test/overlay.test.js --stress`。
- **即时生效型设置必须即时落盘（v0.2.25，加这类偏好时必须照做）**：字号、语速这类"点一下就看到/听到效果"的偏好，**不能挂在「保存设置」按钮上**。设置弹窗有 ✕、安卓返回键、Esc、点遮罩四种关法，只有一种会走保存按钮——挂在保存上等于四分之三的关法都会丢设置。两条规则：① 点一下立刻写 `Store`（走 `commitProfile()`，同训练页阶段 chip 的 `Store.save()` 范式）；② 选中态**只从 `Store` 派生**（`segGroupHTML()` 渲染 + `bindSegGroup({ current, commit })` 重刷），绝不用 DOM 上残留的 `.active` 反推真值。用 `.active` 反推是 v0.2.25 修掉的那个 bug 的根源：DOM 与 `Store` 成了两个真相来源，点完特大再用 ✕ 关掉，字号已生效但没落盘，重开回显"标准"、刷新后字号直接掉回标准。相关回归必须跑 `node test/settings.test.js`。
- **同类条目用分组列表，不要一条一张卡（v0.2.26，加列表/按钮时必须照做）**：同一页出现三个以上同构条目（训练动作、科普文章）时，用「分组标题 + 一张卡内分行」（`.section-label` + `.list-group` / `#ex-list` + `.ex-item`），不要每条一张带阴影的浮动卡片——十几张同款卡叠起来是"卡片墙"，看不出哪项是今天该做的。按钮分三级：**实心蓝（`.btn`）每屏只允许一个主操作**（保存、拨 120），列表行里重复出现的操作用浅蓝（`.ex-start` / `.ci-action`，`--primary-soft` 底 + `--primary-dark` 字，对比度约 6.4:1），完成态用浅绿。一屏六七个实心蓝按钮会让真正的主操作消失在其中。触控目标仍是 48px 起，分级只改颜色不改尺寸。
- **重渲染不要跳页首**：`render(view, { keepScroll })`。切页用默认（回顶部），"数据变了重渲染"（如训练打卡）传 `keepScroll: true`——否则在训练页往下翻着练，练完一个就被弹回顶部。
- **训练引导器 `openTrainer(ex)`**：全屏覆盖层，按 `ex.mode.type` 三种形态：
  - `reps`：大圆按钮计次，**大数字是"还差几次"的倒数**（不是已完成数），到量显示 ✓、超量说"比目标还多 N 次"，达标震动+提示音；
  - `timer`：倒计时（开始/暂停/重置）+ 人话时长与进度条，归零提示；
  - `game`：挂载认知游戏，游戏内"完成打卡"回调 `finish()`；另传 `onSwitch(gameKey)`（换成别的游戏）与 `onQuit()`（中途收工也打卡）。
  - 完成 → `Store.logExercise(id)`（同日去重）→ toast → 关闭 → 重渲染。
  - **关闭时必须清理**：`closeTrainer()` 负责 `clearInterval` + `Games.stop()`，新增异步资源要在这里一并清理。
- **历史视图**：三个入口都是 `openModal` 弹窗，不占主页面高度——`openVitalHistory(kind)`（趋势图 + 全部记录 + 状态点 + 删除，删除后 `paint()` 重画弹窗并 `renderRecords()` 刷新背后页面）、`openExerciseHistory()`、`openMedHistory()`。血压/血糖/体重的判定统一走 `vitalStatus(kind, v)`（复用 `bpBadge/gluBadge/bmiBadge`），趋势图统一走 `drawVitalChart(kind, canvas, recent)`——**加新指标时改这两个函数即可**。打卡日历 `calendarHTML(n)` 由 `Store.exerciseCalendar(n)` 驱动，训练页与弹窗共用。
- **安全/转义纪律**：所有**用户输入**（姓名、药名、剂量、备注等）插入 HTML 前必须过 `esc()`。`data-*.js` 里的静态内容是我们自己写的，直接插入；**如果未来文章/动作内容改为用户可编辑或远程下发，必须改为全量转义或消毒**。
- 提示反馈：`toast(msg)`（5s 自动消失）；需用户处理的错误用 `showError()` 持续显示，存储问题另由 `renderStorageNotice()` 显示；`beep()`（WebAudio 提示音+震动，失败静默）。

### storage.js

纯数据层，无 DOM 依赖（因此可以在 Node 里测试）。公开 API 见文件头部注释和 `return` 清单。`save()` 在规范化前检查容量、保存前对照 `persistedRaw` 检测过期快照，失败回滚到 `persistedJSON`；不会自动合并另一标签页的数据。`exportBackup()` 也走解析校验，拒绝不可恢复的文件。改这里必须同步跑 `node test/storage.test.js`。

### data-exercises.js

训练动作 schema：

```js
{
  id: 'bobath',            // 全局唯一，打卡记录引用它，【发布后不要改 id】否则历史打卡对不上
  cat: 'limb',             // limb|hand|speech|swallow|cognitive
  stage: 'bed',            // 仅 limb 类需要：bed|sitting|standing|walking
  icon: '🙌',              // emoji 图标
  name, goal, dose,        // 名称 / 目的 / 建议量（纯文本）
  mode: { type: 'reps', target: 10 },     // 或 {type:'timer', seconds:300} 或 {type:'game', game:'memory'}
  steps: ['...'],          // 分步要领，患者视角、口语化
  caution: '...',          // 安全警示（可省略，但站立/步行/吞咽类必须有）
}
```

`DAILY_PLAN` 定义各阶段的每日推荐组合（4~6 项，覆盖肢体+手/言语+认知）。新增动作后酌情加入。

### data-articles.js

文章 schema：`{ id, icon, group, title, sub, body }`。`body` 是 HTML 字符串，可用的语义类：`<h3>` 小节、`.art-tip`（绿色提示框）、`.art-warn`（红色警告框）。分组按 `group` 字符串自动聚合，顺序 = 首次出现顺序。

### charts.js

`Charts.line(canvas, seriesArr, opts)`。`seriesArr: [{label, color, values: [{x:'MM-DD', y:Number}]}]`；`opts.refLines: [{y, color, label}]` 画目标参考虚线。已处理 devicePixelRatio。X 轴按索引均分（非时间比例尺）——对"最近14条记录"这种用法是正确的简化。

### games.js

`Games.start(key, container, onDone, opts)`；`onDone(score, detailText)` 由游戏的"完成打卡"按钮触发。`opts = { onSwitch(gameKey), onQuit() }` 供"换个不用算的""先打卡收工"使用——**游戏层不碰 `Store`**，打卡与打开别的动作都由 app.js 执行。**新增游戏**：写 `function mygame(container, onDone, opts)`，结束时调 `showResult()`，注册进 `registry`，再到 data-exercises.js 加一条 `mode: {type:'game', game:'mygame'}` 的动作。游戏内的 `setInterval` 必须存入模块级 `timerId`（`Games.stop()` 靠它清理）。

**「算一算」（`math`）的设计约束**——这是最容易让患者产生逆反心理的一处，改动前请读 §6 的"少算数原则"：三档难度自选且随时可换（`MATH_LEVELS`，`mid` 档刻意**不产生进位/退位**）；**答错不判错不记分**（第一次橙色"再看看"允许重选，第二次给答案 + `mathExplain(q)` 的分步拆解）；「看提示」「这道先跳过」都不扣分不变红；连错两道自动降到容易档；结果页只报"做完几道 / 自己算出几道"，**不出现比分**。`mathExplain` 有凑十、退十、整十、借十四种讲法，改它必须穷举校验文案里的每个算式与中间结果（曾出现"先减 0 回到整十"这类错误 670 例）。

### speech.js

`Speech.speak(text, {rateKey, onEnd, onFail})` / `stop()` / `speaking()` / `supported()` / `rateOf(key)` / `splitSentences(text)` / `normalize(text)` / `voices()` / `setVoice(name)` / `voiceName()`。Web Speech API，零依赖。app.js 侧用 `speakBtnHTML(id, label)` + `bindSpeak(btn, getText)` 接线，朗读稿由 `exerciseSpeechText(ex)` / `articleSpeechText(a)` 生成。

**"听起来生硬"是三层问题，不是音色问题（v0.2.27）**。用户回报"配音太生硬"，本能反应是换音色，但实测三层里音色是影响最小的一层：

1. **喂给引擎的文本（主因）**。屏幕上的写法是给眼睛的，原样丢给 TTS 就是机器味的来源：`10～15次` 念成"10 15次"（范围整个听不出来）、`mmHg` 拼成"m m h g"、`10次×2组` 念成"10次乘2组"、`130/80 mmHg` 念成"130斜杠80"、`——` 引出的补充说明被跳过或念出怪音、`阿司匹林+氯吡格雷` 的加号念不出来。`normalize()` 里的 `SPEAK_RULES` 逐条换成口语说法，**界面显示一个字都不改**（数据文件不动）。规则表**顺序敏感**，改动前读表上的注释：含 `/` 的单位必须排在裸 `/` 规则之前（否则 `mmol/L` 先变成"mmol或L"）；去 emoji 的码位区间覆盖了箭头，必须排在箭头规则之后（否则 `→` 被直接删掉、递进关系丢失）。两条易犯的错：① 吃单位/破折号前后的空白只能用 `[ \t]`，**用 `\s` 会连换行一起吃掉**，把上下两句粘成一句（这条踩过，被测试抓出来）；② 乘号规则只能要求右边是数字——动作库里 7 处乘号左边全是量词（`10次×2组`、`5～10个台阶×2～3回`），要求左边也是数字会一处都匹配不上。
2. **句间没有换气**。原先是"上一句 `onend` 立刻 `speak` 下一句"，机关枪式。现按标点给不同长度的停顿（`GAP_SENTENCE` 320ms / `GAP_CLAUSE` 130ms，`gapAfter()` 判断）。**停顿定时器也必须认代号**：停顿期间用户点了停止或换了内容，定时器到点不能把旧队列接着念下去。
3. **文章的块级边界**（在 app.js 侧）。`articleSpeechText` 原先用 `textContent` 直取，`<h3>2. 控制血压</h3><ul><li>高血压是…` 被粘成"控制血压高血压是…"，一句破句念到底。改成 `collectSpeechBlocks()` 按块级标签断行、段末没标点补句号。**只在"叶子块"上取文本**（`node.matches(SPEECH_BLOCK) && !node.querySelector(SPEECH_BLOCK)`），否则 `ul`/`div` 这类外层容器会把内层每一段重复念一遍。实测 9 篇文章切出 10~21 块。

**音色可选**：有些系统装了多个中文音色，默认那个不一定最好听。`voices()` 只列中文音色（`/^zh/`）；音色名对老人没有意义，`VOICE_ALIAS` 按 **token 子串**匹配给中文说法（不能用整名相等——iOS 是 `Tingting`、安卓是 `Chinese China`、Windows 是 `Microsoft Xiaoxiao Online (Natural) - Chinese (Mainland)`，整名相等只认得出 iOS 那一种，其余会把一长串英文塞进按钮），认不出的走 `shortLabel()` 去掉厂商前缀与括注、实在没人名就用"中文语音"兜底。设置页只在 `voiceOpts.length > 1` 时才显示这一档（只有一个声音时让人"选"是无意义的噪音）。存 `profile.speechVoice`（自由字符串，**不能白名单**：每台机器装的音色不一样，枚举不出来），机上不存在时 `pickVoice()` 静默回落到 zh-CN → 任意中文，换手机或恢复别人的备份都不会哑掉。凡是整体换掉 `data` 的地方（启动、恢复备份、撤销恢复、清空）都要 `applyVoice()` 重新套一次——和 `applyFont()` 同一个道理。

**五个已踩过的坑，改这里前务必知道**：

1. **音色异步加载**：Chrome 首次 `getVoices()` 返回空，要监听 `voiceschanged` 重挑中文音色。
2. **长文本被截断**：部分浏览器对单条 utterance 有长度限制 → `splitSentences()` 按句切分排队朗读。
3. **`onend` 偶发不触发**：队列会卡住 → 按字数估算时长的看门狗定时器兜底推进。
4. **API 齐全但一个音色都没有**（部分微信内置 WebView、未装语音包的系统）：`speak()` 静默失败，`supported()` 仍为 true。因此**不能只靠 `supported()` 判断能不能出声**——`speak()` 内有 1.5 秒起播看门狗，没等到 `onstart` 就调 `onFail`，UI 负责复原按钮并提示"这个手机好像没装朗读语音"。**不要**加"本机是否有语音包"的查询接口：`getVoices()` 为空时无法区分"还没加载完"和"根本没有"，给不出可靠答案。
5. **不能用 lookbehind**（`(?<=)` / `(?<!)`）：老 WebView 不支持，会让**整个文件解析失败、朗读功能全哑**。需要"只在左边是某类字符时替换"就用捕获组把左边那个字带回来（见加号规则）。contracts 与 speech 测试都钉了这条。

**为什么不换成"更好的 TTS 库"（v0.2.28 选型结论，再被问到直接引这段，不用重新调研）**：本项目的三条红线——零依赖+`file://` 可用、隐私红线、CSP `connect-src 'none'`——几乎排除了全部神经 TTS。

| 方案 | 本质 | 为什么不行 |
|---|---|---|
| Azure AI Speech / edge-tts | 文本发到微软服务器 | 违反隐私红线，且 CSP `connect-src 'none'` 直接禁掉 |
| SpeechT5 / Kokoro / Piper | 浏览器端 ONNX+WASM 离线推理 | 要么需下载数十~上百 MB 模型（同样被 CSP 禁，得放宽才能下），要么需打包进部署+构建步骤（破零依赖）；老人机加载与推理都吃力 |

**要用神经 TTS 就必须先松一条红线，这是产品决策，不是技术选型——必须先问用户。** 不破红线的替代路径已在 v0.2.28 落地：`openVoiceGuide()` 教用户在手机系统里下载更自然的中文语音包（iOS 带「增强／高质量」的声音、安卓的中文语音数据），装一次长期可用，**音质提升远大于应用层能做的任何调整**。引导措辞只给"大致这样找"+ 搜索关键词，不给精确按钮名：各机型/版本菜单名不同，给老人一个找不到的按钮名比不给更糟。

**必须 `Speech.stop()` 的时机**：关训练引导页（`closeTrainer`）、关任意弹窗（`openModal` 的 `close`）、切换主页面（`go`）。漏了会在 iOS 上继续念上一页的内容。

`node test/speech.test.js` 是这层的护栏（**改 speech.js 或朗读稿必跑**）。它不只测样例，还拿真实动作库+文章库**全量扫**（47 条）确保没有漏网的写法；用桩 `speechSynthesis` 记时间戳**真的量出句间停顿**（不是看源码里有没有写）；并反向断言数据文件里给眼睛看的 `～`/`×`/`mmHg` 必须还在——**朗读稿变了但显示文案一个字都不能改**。

### figures.js

三层结构，**不要手填坐标**：

1. `FIG` —— 骨架与画法引擎。`SEG` 按成人 7.5 头身折算各肢段长度（头 12 → 全身 90；来源见 docs/RESEARCH.md §十）；`at(from, deg, len)` 沿角度走一段得到关节；`joint(a, c, l1, l2, dir)` 是两连杆逆解——**已知髋与脚、反求膝**，这是"抬臀时脚必须钉在原地"的关键；`figure(pose)` 把姿势画成 SVG；`foot(an, toe)` 把脚画成楔形。
2. `POSES[id] = { alt, props, arrow, frames:[poseA, poseB], labels, ... }` —— 姿势用 `at`/`joint` **算**出来，因此肢段长度由构造保证一致。局部帧 0..104 × 0..118，y 向下，地面/床面 y=100。
3. `FIGURES[id] = { alt, svg }` —— 加载时渲染（左帧 `translate(4,0)`、右帧 `translate(128,0)`，画布 240×140）。app.js 只用这一层：`figureHTML(ex)` 有图就画、没图退回大 emoji，**不留空位**。

`node test/figures.test.js` 是这套东西的护栏，**改 figures.js 必跑**。它把"画得对不对"变成可断言的几何性质：每帧肢段长度等于骨架定义、两帧必须是**同一个人**（同名肢段长度相等）、关节不穿地、两帧差异足够大（否则小尺寸看不出动作）、key 必须对得上真实动作 id，再加每个动作的医学要点（踝泵勾脚绷脚夹角 ≥60° 且不穿床垫、Bobath 双手必须过头顶且与头横向拉开 ≥8、桥式肩脚不动且肩髋膝近似共线、坐到站髋明显升高躯干竖直、踏步抬起侧膝高于支撑侧且脚离地）。**它第一次运行就抓出一个真 bug**（踏步站立帧的远侧腿被画到了髋以上）。

画法约定：线条 `currentColor`（跟随主题色与字号）；关节实心圆点；颈部画到头的圆周为止（连到圆心会在头里留一根杆）；脚用楔形不用线（跖屈时脚与小腿几乎共线，线看着像"腿变长了"）；虚线只用于对齐提示；同一部位两帧只改角度、不改形状。`alt` 既给读屏也印在图下方，必须能独立说清动作。

⚠️ **医学示意内容**：姿势要点取自 data-exercises.js 里已按指南核对过的 `steps`/`caution`（来源见 docs/RESEARCH.md §八），**仍需康复医生复核**，尤其患侧摆位与关节角度。宁可不画也不要画错。

**人眼检查是必需的，几何断言替代不了它**（AI 写坐标是盲画）。用 `node test/preview-figures.js` 生成 `figures-preview.png`（同时给 400px / 150px / 96px 三种尺寸），再 `node test/preview-figures.js <动作id>` 逐张放大看。这轮靠放大才发现的问题：整条腿悬空在床面上方 12 个单位；跖屈的脚穿进床垫里；严格侧面直上举时手和头挤成"两个圆圈"（改成符合 Bobath 做法的前屈上举后解决）；脚画成线几乎看不见；虚线残影放大后是一团碎线（已删）。

## 7. 测试

```bash
# 1) 数据层单元测试（快，每次改 storage.js 必跑）
node test/storage.test.js

# 1a) 可选密码加密备份（Web Crypto 往返、错误口令与篡改拒绝）
node test/encryption.test.js

# 1b) 跨模块契约检查（少算数、触控尺寸、CSP、动态属性转义）
node test/contracts.test.js

# 1c) 简笔画几何校验（快，每次改 figures.js 必跑）
node test/figures.test.js

# 1c-2) 朗读层（归一化规则 + 真的量出句间停顿 + 音色回落；改 speech.js 或朗读稿必跑）
node test/speech.test.js

# 1d) 真实 Chromium 浮层回归（关闭/普通下载/进入加密/完成加密后页面不得离开）
node test/overlay.test.js --stress
# 部署后可把生产地址作为参数，在隔离浏览器中跑同一套回归
node test/overlay.test.js https://stroke-rehab-assistant.pages.dev/

# 1e) 备份压力回归（40 轮浮层/历史 + 4.8MB 级备份加密恢复）
node test/overlay.test.js --stress
node test/backup-stress.test.js

# 1g) 设置项持久化（字号/语速：不按保存就关掉也必须留住，且回显与实际一致）
node test/settings.test.js

# 1f) 简笔画目视检查（改 figures.js 后必看一眼图，断言替代不了眼睛）
node test/preview-figures.js            # 全部动作 → figures-preview.png（含 400/150/96px）
node test/preview-figures.js bobath     # 单个动作放大看

# 2) 冒烟测试：无头浏览器渲染 5 个页面 + file:// 直开 + 静态资源可达性
bash test/smoke.sh
#    依赖 Playwright 下载的 chrome-headless-shell（无需安装 playwright 包）
#    没有时：npx playwright install chromium
#    浏览器与网络请求均有硬超时；脚本会清理临时目录和自己启动的服务器。

# 3) 语法快查
for f in js/*.js; do node --check "$f"; done
```

手工回归清单：**唯一真源是 [`docs/MANUAL-TEST.md`](MANUAL-TEST.md)**（52 项，按真机执行顺序分三轮 + P2 选测，含时长预估、新版判据、安全注意与历次实测结果）。

> 这里**故意不再放第二份清单**。此前本节与 MANUAL-TEST.md 各自维护，已分叉到 41 条 vs 45 条——本节独有 7 项（`?view=` 直达、日历颜色变深、停用药报告小节、朗读不念 HTML 标签、一局游戏、导出可复制、**简笔画的医学确认**）在真机执行版里根本不存在，等于悄悄漏测。这 7 项已于 2026-09-19 合并进 MANUAL-TEST.md。要加测试项就加在那一份里。

自动化测不到、只能靠真人的四类（真机清单里已分别标注）：**安卓返回键**、**语音朗读的出声与听感**、**特大字号下的实际观感**、**简笔画姿势的医学正确性**（必须找康复医生/治疗师）。

## 8. 医学内容维护守则（重要）

1. **来源**：训练动作与文案参考《中国脑卒中康复治疗指南》《中国缺血性卒中和短暂性脑缺血发作二级预防指南》等公开指南的通行建议。修改医学内容需给出指南/权威来源依据，在 PR/提交说明里注明。
2. **不越界**：本应用是健康教育与自我管理工具。**永远不要**：给出个体化用药建议（只做"遵医嘱记录与核对"）、承诺疗效、弱化就医提示、删除免责声明和安全警示。
3. **数值有主**：文中出现的目标值（血压耐受时<130/80、LDL-C<1.8mmol/L、限盐5g、双抗21天等）均为指南一般性建议，展示时必须伴随"具体遵医嘱"措辞。指南更新时集中检查：`data-articles.js`（prevention/diet/followup/positioning/rehab-principle 五篇）、`app.js` 的 `bpBadge/gluBadge/bmiBadge` 阈值与提示文案、README。各数值的指南出处见 `docs/RESEARCH.md` §三（含 130/80 的例外分支、HbA1c 不做硬指标等注意事项，改文案前必读其 §八）。
4. **语言**：面向老人的口语化中文，避免术语堆砌；安全警示用"必须/立即/不要"等明确措辞。

## 9. 已知限制与后续路线

**优先级硬规则（2026-08-10 用户确认）**：先处理会影响患者能否正确、安全、持续完成康复的交互与信息准确性，再做有明确恢复收益的功能；备份、离线、平台迁移等便利性工作排在后面。医学内容必须先有权威依据和专业复核，不能用“功能更多”替代准确。

当前明确不做/做不了（对应 §2 的取舍）：

- 无云端同步、无多设备互通（换手机数据不跟随；缓解手段是导出功能）
- 无后台推送提醒（纯网页做不到可靠的定时提醒；页面内核对 + 建议用户设手机闹钟）
- 图表为最近 N 条的索引轴，不是严格时间轴
- 全部训练有文字分步和语音朗读，但仅 5 个动作有两帧示意图、无视频；示意姿势仍需康复医生/治疗师复核后才能继续铺开
- ~~训练/用药/游戏历史无应用内视图~~ ——已补齐（v0.2.9，见 §10）。~~**遗留取舍**：服药历史每天的"应服次数"按当前药物清单计算，改过处方后更早日期的分母会跟着变~~ ——**已解决**（v0.2.15：药物带 `from`/`to`，计数走 `Store.medsOn(date)`，停药/加药不再影响历史日期的分母）。**剩余取舍**：同一种药中途改剂量或改服药时间点没有版本记录，改完之后历史日期会按新的时间点显示
- ~~记录页历史列表位于输入表单下方、小屏易漏~~ ——已解决（v0.2.9：入口上移到"最近血压/血糖/体重"卡内，全部记录进弹窗）
- 训练日历固定看最近 4 周；训练历史弹窗一次渲染全部有记录的日期，长期使用（数百天）后需要分页或折叠
- **v0.2.12「少算数」的剩余缺口**：血压/血糖**录入**仍要求患者读血压计并输入数字（这是数据源头，无法回避；可考虑的缓解是"上次是 138/86，这次差不多吗"式的就近微调输入）；导出给医生的报告仍是精确数字与百分比（**这是刻意保留的**，医生需要原始数据）；训练日历格子里仍是当天项数的数字（颜色深浅已是主要通道，数字只作补充）。语音朗读已在 v0.2.13 完成；当前更重要的是用真机和真实患者验证可读性、朗读可用性及操作负担，并由专业人员复核训练示意和医学文案

建议的演进顺序（先看患者恢复价值与风险，再看开发成本；调研依据见 docs/RESEARCH.md §五/§六）：

1. **真机交互与医学准确性复核**：邀请患者/家属走完今日任务、训练引导、服药、记录和急救入口；重点观察读字、误触、返回键、语音、疲劳与抗拒。康复医生/治疗师逐项复核训练示意、阶段适用性和安全警示
2. **短期双抗疗程提醒**：仅针对医生已开立并登记了结束日期的短期疗程，在临近日期提示“请按医嘱复诊确认”，绝不自动建议停药或改单药；实施前再次核对指南适用人群与例外
3. **动作示意图专业复核后扩充**：画法体系已完成（v0.2.14），目前 5 个动作；几何测试只能保证画法自洽，不能替代治疗师确认患侧摆位、关节角度和阶段适用性
4. **卒中后情绪筛查与就医指引**：PSD 常被忽视，但量表措辞、危机响应与转介路径必须先由专业人员审核；不能只做一个分数
5. **患者/家属双视图**：患者视图进一步减少干扰，家属视图保留趋势、原始数字和安全须知；先用真实家庭分工验证是否有价值
6. **Service Worker 离线缓存**：弱网/无网可用；属于可靠性与便利性提升，需处理缓存版本，避免医学内容更新后仍展示旧版本
7. **久坐提醒**：先按行动能力和跌倒风险设计适用条件，不能对所有卒中患者统一提示起身活动
8. **家属远程查看**：需要后端并扩大隐私与合规边界，只有真实家庭需求明确后再考虑
9. **微信小程序壳**：意味着第二套发布与审核链路，不在近期主线

已完成的基础能力：语音朗读（v0.2.13）、数据备份/恢复（v0.2.6）、恢复点与撤销（v0.2.20）、可选密码加密备份（v0.2.21）。

## 10. 变更记录

> **写这里之前先读 [`AGENTS.md`](../AGENTS.md) 的「写文档的纪律」**：一条 ≤800 字节，只写「改了什么 / 为什么 / 跑了哪些验证 / 未验证的是什么」。过程叙事不进这里——会再咬人的坑写成 §6 的约定或一条测试断言。
>
> **v0.2.28 以前的条目是长篇遗留**（最长 5154 字节，整节曾占全文 60%），**不要拿它们当范本**。它们没有被回头重写：那些踩坑细节只有一部分提炼进了 §6，批量删改会丢掉真东西，需要逐条核对才动。

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-25 | v0.2.30（本地） | 应用户要求参考 Linear 打磨：统一表面、标题与控件；宽屏侧栏、手机底栏、窄屏按钮分行；修复打卡/修改后返回焦点丢失，补读屏按钮名称与组件样张。验证：contracts/speech/settings/overlay --stress/smoke、语法、90 组页面与触控、18 组设置/急救、缩放保留输入、截图目验通过；窄屏收尾后复跑 overlay。未验：真机读屏、原生键盘/手势；未部署。 |
| 2026-09-25 | v0.2.29 上线 | 用户要求“处理完”：沿用现有部署脚本发布到原 Pages 地址，部署 `0457801b`，未改域名。生产 12 个运行时文件与本地一致，安全响应头在位；线上 settings 与 overlay --stress 全通过，含返回/草稿/失败重试/备份恢复/急救、72 组布局及 40 轮压力。未验：真机拨号、读屏和文件落地；域名迁移待定名。 |
| 2026-09-25 | v0.2.29 | 应用户要求夯实底座：建立设计系统与组件样张；帮助/备份保留设置草稿、原页与焦点；保存先校验，损坏原件保护、过期页面覆盖检测、超限拒绝；备份可恢复校验、8MB 计数修正、异步取消及同文件重选；急救覆盖浮层并前置拨号。补子域名迁移步骤。验证：storage/contracts/speech/encryption/backup-stress/settings/overlay --stress/smoke、语法、60 组页面及 12 组设置/急救布局、截图目验通过。未验：真机拨号、读屏、域名实迁。 |
| 2026-09-19 | v0.2.28 | 朗读「换更自然的声音」引导 + 图表色去重复。① 新增 `openVoiceGuide()`：教用户在**手机系统里**下载更自然的中文语音包（iOS 增强音／安卓中文语音数据）——提升听感最有效且不破红线；入口在设置页语速说明下方（限 `Speech.supported()`），措辞只给"大致这样找"+搜索关键词、不给精确按钮名，顺带覆盖"微信里点了没声音"。② `showVitalTooltip` 不再硬写 hex，改从 `VITAL_SERIES` 取色。③ 订正 §4 行数。神经 TTS 选型结论见 §6 speech.js。回归：四套 Node + settings/overlay + smoke 全过（含补跑 v0.2.27.2 未跑的无头套件）。**未验**：引导菜单措辞、装包后听感。 |
| 2026-09-19 | v0.2.27.2 | 可维护性：`js/app.js` 加分区导航（顶部「模块地图」目录 + 各视图区 `【区】` 前缀可搜索跳转），纯注释、**未移动任何函数、未改加载顺序**。起因：app.js 已从交接时约 1200 行长到 2100+，缺总览入口。当时判断"仅改注释无运行时影响"而未跑无头套件，**已在 v0.2.28 补跑**；遗留的 §4 行数也已订正。 |
| 2026-09-12 | v0.2.27.1 | 文档漂移审计 + 朗读规则潜伏 bug（无用户可见变化）。① 文档引用扫描报出 8 处"幽灵引用"，查证**全是误报**（否定句/通称/历史记录三类），教训见 AGENTS「写文档的纪律」。② 换问法"文档承诺的行为还成立吗"抓到真 bug：`SPEAK_RULES` 有 6 条用 `\s` 吃空白，违反自己第 12 条约定（`\s` 吞换行粘句）；当前内容 0 触发属潜伏 bug，新增以 `·`/`→` 开头的要点就会爆。10 处全改 `[ \t]`；护栏升级为**遍历 `SPEAK_RULES` 全表 + 静态禁 `\s`**，新规则自动纳入。③ 收敛速查文件分叉（`ef5a439`）：`AGENTS.md` 定唯一真源、`CLAUDE.md` 改指针。回归：六套 Node + settings/overlay + 两探针 + smoke 全过。 |
| 2026-09-12 | v0.2.27 | 朗读自然度改造（用户："配音太生硬"）。主因不是音色而是喂给引擎的文本，三层一起改：① `Speech.normalize()`（`SPEAK_RULES`）把屏幕写法换口语（`10～15次`/`mmHg`/`×`/`130/80`/`+`），**显示文案一字不改**；② 句间按标点加停顿；③ `articleSpeechText` 改 `collectSpeechBlocks()` 按块级断行，不再把标题正文粘成破句；④ 音色可选（本机 >1 中文音色才显示，即时落盘、不存在回落）。新增硬约定：禁用 lookbehind。原理与坑见 §6 speech.js、HANDOVER §3。验证：新增 `test/speech.test.js`（全量扫 47 条 + 桩引擎量停顿）、contracts +3、两探针、六套 Node + settings/overlay + smoke 全过。已上线 `b8aee3fd`。**未验**：iPhone 实际听感。 |
| 2026-09-12 | v0.2.26 | 视觉层级重设计（用户回报"界面不太满意"，未指明具体项）。带数据截图通读全部页面后定位三个问题：① **训练页是一面"卡片墙"**——16 个动作各自一张浮动小卡片、每张右侧一个实心蓝「开始」，一屏六七个同款蓝块，看不出哪项是今天该练的；② **实心蓝滥用**，真正的主操作（保存记录、打卡、拨打 120）被同样的蓝淹没；③ **分组标题过弱**，知识页七个分类是一行小灰字，起不到扫读锚点作用。注意今日页的动作列表**本来就是**"一张卡内分行"，说明范式在项目里已有先例，训练页没跟上。改动：① 训练库 `#ex-list` 改为分组卡容器，`.ex-item` 从浮动卡片变成卡内行（发丝线分隔、`:last-child` 与分组标题前不画线）；两条分组说明从内联 `.muted` 小灰字提为 `.ex-group-label`（800 字重 + 字距）。② `.ex-start` 从实心蓝改 `--primary-soft` 底 + `--primary-dark` 字（#1F55B5 on #E8F0FE ≈ 6.4:1，高于 4.5:1 要求），与今日页 `.ci-action` 同一口径；`.ci-action` 字色也从 `--primary` 统一到 `--primary-dark` 并加粗。**实心蓝此后只留给每屏唯一主操作**。③ 知识页新增 `.section-label` 分类标题 + `.list-group` 分组卡，同款行内布局。④ `exItemHTML()` 里那串写死行样式的内联 `style` 删掉，行样式统一由 CSS 给（今日页与训练库共用同一套 `.ex-item`）。⑤ 顺手修 `.delta-line .muted` 改 `inline-block`，「（上次 2026-09-05）」放不下时整个括号一起换行，不在日期中间断开。适老化底线未动：字号、48px 触控目标、对比度只升不降。验证：108 组合 UI 审计（3 档字号 × 2 档屏宽 × 5 视图+12 弹窗，带数据）——整页横向滚动 0、元素越界 0、文本裁剪 0、流程点击失败 0（仅剩长剂量文案的正常两行折行提示，非缺陷）；11 张改后截图逐张目验训练/知识/今日；storage/contracts/figures/encryption/backup-stress/settings/overlay/smoke 八套全过。运维备忘：仓库外审计探针的按钮文案已过期（找「查看全部记录」，实际是「查看全部历史记录（N 条）」），已修正；审计汇总每类只打印前 30 行，WRAP 有 930 处时打印的恰好全是 `normal/375`，别误判为只跑了一个组合。同日上线：生产部署 `528e23d2`，线上 `css/style.css` 与 `js/app.js` 与本地逐字节一致（index.html 带 `ver=20260912180207`），CSP/HSTS/防嵌入头在位。核对时踩到一次假警报：`pages deploy` 刚结束时生产别名取到的仍是上一版（边缘缓存），加随机 query 穿透后即为新版——**别据此重复部署**。**真机待复测**：训练页与知识页在 iPhone 上的观感。 |
| 2026-09-12 | v0.2.25 | 修复设置项静默丢失（用户："字体选了特大，重新打开却显示标准"）。根因不是模板默认值，而是**即时生效的偏好被挂在了「保存设置」按钮上**——弹窗四种关法只有一种走保存，其余三种让已生效的字号没落盘（刷新后掉回标准；语速同 bug）。固化为硬约定 #9，做法见 §6「即时生效型设置必须即时落盘」。改动：① 抽 `segGroupHTML()`/`bindSegGroup`，选中态只从 Store 派生、点一下即 `commitProfile()`；② 保存按钮不再读字号/语速；③ radiogroup + 方向键；④ 选中态加 ✓ 不只靠颜色。测试：新增 `test/settings.test.js` + contracts +5，反向验证修复前 5 条全 FAIL。已上线 `d52966a6`。 |
| 2026-09-12 | v0.2.24.2 | 用户回报 v0.2.24.1 后"问题似乎还存在"。排查发现真正的 iOS 根因（桌面 Chromium 完全不可见）：**iOS Safari 会给原生日期/时间控件强制绘制自己的灰色渐变底+内阴影（用户所说的"阴影"），且该原生外观自带引擎最小宽度，无视 CSS 的 width/min-width/max-width**。① 修复：`@supports (-webkit-touch-callout: none)`（仅 iOS Safari 命中）内对 `input[type=date]/[type=time]` 设 `-webkit-appearance:none`——去掉原生外观后控件才完全遵守我们的盒模型（我们自绘边框/底色不变，点击弹出的仍是非系统转盘）；桌面浏览器保留原生日历图标不受影响。② 顺带解决"改了看不到"：index.html 全部 css/js 引用加 `?ver=1`，deploy.sh 部署时用 sed 把 `?ver=` 统一替换成部署时间戳（只改临时副本，仓库不动）——iOS Safari 缓存激进，此前上线后手机可能一直拿旧 css/js，今后每次部署自动穿透。验证：contracts/audit 108 组合（0 横向滚动/越界/裁剪）/smoke 全过。上线：生产部署 `614894a2`，线上 index.html 已带 `ver=20260912120029`、css 已含 iOS 规则。**待用户真机复测**：日期/时间的灰色原生底和超出应已消失。 |
| 2026-09-12 | v0.2.24.1 | 原生日期/时间控件溢出的结构性保险（用户以旧版截图指出："日期/时间选择的阴影超出应用界面"——那是 v0.2.23 前横向撑破的外在表现，新版已无那两行；但由此确认了一类桌面 Chromium 验证不到的 **iOS 特有风险**：原生日期控件有引擎强制最小宽度，容器挤到比它窄时框会戳出卡片）。三道保险：① `input, select { min-width:0; max-width:100% }`——任何输入框结构性禁止超出容器（最坏变成容器内裁剪，绝不戳破卡片）；② `.field.wide { flex:1 1 100% }`——展开后的日期/时间永远各自独占整行（全宽下 iOS 的 "Sep 12, 2026" 长格式也放得下），替代原先"并排放得下就并排"的策略；③ `input[type="date"].set-input { min-width:9.5em }`——设置页发病日期在特大字号下退化为独占一行，行内放得下时仍与标签同行。验证：常驻审计探针（已移至仓库外 `Dev/debug/test/ui-audit.js`，传项目根目录参数复用）复跑 108 组合全绿；展开态/设置页 4 张截图目验（标准字号展开后日期/时间全宽、年份完整）；全套单测+overlay+smoke 通过。同日上线：生产部署 `dfa5fb44`，线上 `css/style.css` 与本地逐字节一致。 |
| 2026-09-12 | v0.2.24 | 记录表单与设置页重设计（用户反馈："既紧凑又宽松，用起来舒服，现在感觉怪怪的"，附两张 iPhone 实测截图，红叉标出脉搏独占整行的浪费感）。调研依据：移动端表单单列+逻辑分组共识（[IxDF](https://ixdf.org/literature/article/ui-form-design)、[UX World](https://medium.com/uxdworld/form-design-best-practices-6b8b5af14c0d)）、低频字段渐进披露（Apple Health 式"记录时间"）、iOS 设置列表的行内布局、[工信部 APP 适老化通用设计规范](http://www.xinhuanet.com/politics/2021-04/07/c_1127302897.htm)的"简约界面/视觉流顺畅"。确立两层信息体系：**主信息=标签在上大字段（相关字段成对），次要信息=标签左控件右的紧凑行**。① 血压/血糖/体重表单：高压/低压保持成对；脉搏从独占整行改为行内字段；日期+时间两大行收敛为「记录时间：今天 11:21」小条（`fmtWhen` 口语化显示今天/昨天/M-D），点按才展开原生日期/时间输入（`bindWhenToggle`，字段 id 不变、保存逻辑不变）；体重表单同构。效果：特大字号下表单+保存按钮一屏放下（此前要滚两屏）。② 设置页：六个输入框去掉 `width:Nem` 定宽改 `.set-input` 弹性填满行宽——任何字号下不再出现"半截输入框"，特大档自然成为标签+全宽输入的堆叠式（iOS 大表单式）。③ 「🔊 听一遍」加 nowrap 不再被折行拆开。④ 320/特大下高压/低压优雅退化为全宽堆叠。验证：重建 108 组合 UI 审计探针复跑——整页横向滚动 0、元素越界 0、文本裁剪 0（修复前 12/120/若干）；8 张关键界面截图人眼复核；storage/contracts/figures/encryption/backup-stress/overlay/smoke 全过。**真机待复测**：记录页三个表单（含点开"记录时间"改日期）+ 设置页在 iPhone 上的手感。同日上线：生产部署 `847ae2ce`，线上 `js/app.js`/`css/style.css` 与本地逐字节一致，浮层四路径对生产回归通过。 |
| 2026-09-12 | v0.2.23 | 同类 UI/UX 问题完整诊断与修复（起点：iPhone SE 2 真机实测发现的 P3「特大字号设置页标签断词」）。用临时探针在真实 Chromium 里对 5 视图 + 12 弹窗 × 3 档字号 × 2 档屏宽（375/320，共 108 组合）体检：整页横向滚动、元素越出右缘、短文本断词折行、文本裁剪；带数据状态（图表/历史/药物行都在有数据时测）。发现并修复四类：① **记录页血压/血糖表单「日期+时间」行在所有字号下都横向撑破页面**（日期/时间输入框有浏览器固有最小宽度且 `min-width:auto` 不可收缩，v0.2 起潜伏，此前 smoke/单测均测不出）——表单重排（脉搏并入高压/低压行）+ `.form-row` 允许换行 + 日期/时间字段 `.field.wide { min-width:8.5em }` 兜底 + 标签括号说明 `.label-opt` 整体换行（不断成"（收缩/压）"）+ 脉搏标签加回（选填）、占位符改回「次/分」防裁剪；② **设置页 `.setting-row` 标签被 em 定宽输入框挤压断词**（真机 P3 根因：输入框 `width:9em` 等随根字号放大，`flex:1` 标签被挤到 ~70px）——标签改 `flex:1 0 auto` 永不收缩断词、行允许换行（特大档下输入框整体落到第二行）；③ **特大字号下头部「脑梗康复助手」标题/「设置」/「急救」在 375 与 320 都断词**——特大档标题绝对字号降为与"大"档一致（1.05rem×22=23px，适老化不回退）、急救键收窄内边距、`.header-inner` 允许换行兜底（截图中两行头部反而更清爽）；④ **特大+320 下连续天数徽章撑破卡片**——`max-width:100%` 允许成两行圆角条。修复后同探针复跑：整页横向滚动 12→0、元素越界 120→0、断词类清零；训练页分类 tab 的横向滚动系 `overflow-x:auto` 设计使然，已识别排除。验证：storage/contracts/figures/encryption/backup-stress/overlay/smoke 全过 + 全部 JS 语法检查通过 + 关键界面截图人眼复核（临时探针已删，截图存 `Dev/debug/test/srehab-frames/fix_*.png`）。**真机待复测**：记录页表单、设置页、头部在 iPhone 实机上的观感。同日上线：生产部署 `88c385de`，线上 `js/app.js`/`css/style.css` 与本地逐字节一致、根路径 index 一致、CSP/HSTS/防嵌入头在、浮层四路径对生产回归通过。运维坑（接 v0.2.9.1）：非交互 shell（`wsl -e bash -c`）PATH 不含 nvm bin，deploy.sh 会回落到系统 node18 的 `npx wrangler@latest` 并误报"未登录"；先 `export PATH="$HOME/.nvm/versions/node/v24.18.0/bin:$PATH"`（并 unset 代理）即可正常。另：`/index.html` 返回 308 到 `/`，逐字节比对应对根路径做。 |
| 2026-09-12 | v0.2.22.4 | 真机实测（iPhone SE 2，iOS Safari 无痕窗口，生产 v0.2.22）：用户提供 4′16″ 录屏，逐帧+音轨复核。**语音朗读首次真机证实出声**（文章约 14s、训练要领约 13.6s，「停止朗读」状态正常）；计次训练闭环（倒数 10→2、圆点、打卡 toast、按钮变「再练」+「今日已完成」）、字号三档即时切换、iOS 原生日期选择器、备份弹窗与隐私提醒、BE-FAST/120 卡、无图动作 emoji 降级均通过；全程无白屏/布局破损。发现 1 个 P3 视觉问题：特大字号下设置页「怎么称呼您」标签折行断词。未覆盖：血压数值输入与保存、备份落地与恢复/撤销、加密备份、用药操作、历史弹窗、图表交互、计时动作、算一算、简笔画动作页、tel:120、微信内置浏览器（结果明细见 `docs/MANUAL-TEST.md` 结果节）。无代码改动。 |
| 2026-09-12 | v0.2.22.3 | 新增 `docs/MANUAL-TEST.md` 真机测试清单：把 §7 手工回归清单按真机执行顺序重排为三轮（无痕窗口全新状态 → 正常窗口带数据主链路 → 微信内置浏览器），标注每轮预计时长、备份/恢复与清空的安全注意（tel:120 只验唤起不真拨、恢复靠恢复点+撤销保护、清空放最后），并给出"确认加载新版"的界面判据（应用内无版本号，以备份弹窗有无「设置密码并加密」为 v0.2.21+ 判据）；§7 加指引。无代码改动。 |
| 2026-09-12 | v0.2.22.2 | 仓库落档（无运行时代码改动）：v0.2.17~v0.2.22.1 的全部工作区改动（含 `_headers` 与 contracts/encryption/overlay/backup-stress 四个新测试文件）此前一直未提交，GitHub 上的代码落后于生产一个月，本次提交并推送补齐。① 新增 `.gitattributes`（`* text=auto eol=lf`）：仓库在 WSL 与 Windows 两侧开发，Windows 侧 Git Bash 的 `core.autocrlf=true` 曾提示把 LF 转 CRLF，不钉死会有全库换行符漂移风险。② `deploy.sh` 可执行位（100755）在 Windows 挂载盘上无法表达而掉成 100644，已在 git 索引中恢复 100755；同盘上 `chmod` 也不生效，故将仓库 `.git/config` 的 `core.filemode` 设为 `false` 消除幻影脏状态（副作用：两侧都不再自动跟踪执行位变更，需要时用 `git update-index --chmod` 显式改；WSL 侧磁盘上的执行位不受影响）。落档前在本机复验：storage/contracts/encryption/backup-stress/figures 五个单测与全部 `js/*.js` 语法检查通过；overlay/smoke 依赖 WSL 侧无头 Chromium，Windows 会话跑不了，最近一次通过为 2026-08-11（v0.2.22.1）。 |
| 2026-08-11 | v0.2.22.1 | 发版后压力收尾（无运行时代码改动）：① `test/overlay.test.js --stress` 在真实 Chromium 连续执行 40 轮设置→备份→关闭/普通下载/加密切换，逐轮断言 URL、DOM、`history.state` 与 `history.length`，历史栈始终只保留一个浮层条目；另用真实快速双击确认普通备份只触发一次下载。② 新增 `test/backup-stress.test.js`，构造 4.86MB（接近 5MB 上限）的完整健康备份，连续解析 12 次、加密/解密各 3 次、恢复/撤销 3 轮均无数据丢失；本机最慢加密 0.35s、解密 0.18s。③ 加固 Chromium 测试夹具：真实鼠标事件、关键点击重试、DevTools 断连和命令硬超时，防止测试静默假通过。④ 纠正旧 `unwindDebt` 文档，改为 v0.2.22 的 `popstate` 统一关闭 + `close.replace()` 原位替换规则。 |
| 2026-08-11 | v0.2.22 | 修复备份入口的严重历史栈回归：设置→备份、备份→加密原先先 `history.back()` 再立即 `pushState()`，在 Chrome 的异步历史遍历竞态下会退到上一页、表现为弹窗/页面“闪退”；普通下载和加密完成关闭也会触发同一路径。① 浮层关闭改为只发起 `history.back()`，由 `popstate` 统一移除 DOM，避免关闭与新弹窗竞态；② 连续弹窗增加 `close.replace()`，原位复用同一历史条目，覆盖设置/备份/恢复预览/药物新疗程等切换；③ 训练引导返回与完成统一走新关闭路径，回退期间禁用浮层控件，防止快速双击重复保存；④ 新增 `test/overlay.test.js`，用真实鼠标事件在 Chromium 中断言四条备份路径 URL 不变、无重载、主导航可用、历史条目不增长且无未捕获异常。此前测试漏报的原因：已有探针覆盖加密和恢复数据正确性，但未断言这四条用户操作后的浏览器历史与页面存活状态。生产部署 `1aa929c3`，线上四路径同样通过，`app.js` 与本地逐字节一致。 |
| 2026-08-10 | v0.2.21 | 增加**可选**密码加密备份，不改变日常数据和普通备份的默认路径：① 使用浏览器原生 Web Crypto，PBKDF2-HMAC-SHA-256（固定 600000 次、每份随机 16-byte salt）派生不可导出密钥，AES-256-GCM（每份随机 12-byte IV + 固定 AAD）同时提供保密性和篡改校验；口令不保存、不上传。② 加密格式固定算法与迭代次数，外部文件不能抬高计算量拖死页面；加密容器先限 8MB，解密后仍须通过原有 5MB、schema、字段和记录数量上限。③ 设置页保持「下载普通备份」为主操作，只在用户主动选择时进入加密；适用场景明确限定为网盘/共享电脑，输入支持二次确认和显示密码，恢复错误可原地重试且不覆盖当前数据。④ 文案不承诺绝对安全：明确忘记密码应用无法找回、简单密码仍可能被猜中；旧明文备份继续直接恢复。⑤ 新增 `test/encryption.test.js`，覆盖正确口令往返、错误口令、密文篡改、固定参数、随机 salt/IV、数据不泄露与明文兼容；真实 Chromium 验证 `file://` Web Crypto、移动/桌面布局、下载、错误口令及恢复点全流程。⑥ 按用户要求把后续路线重排为“真机交互与医学准确性 → 明确恢复收益 → 便利性 → 平台扩展”，并纠正文档中仍写“无图片/语音”“下一步做语音”的过期状态。安全功能保持次要层级，不挤占训练、记录和医学信息主流程。 |
| 2026-08-10 | v0.2.20 | 备份恢复事务加固：① 外部备份在 `JSON.parse` 前按 UTF-8 严格限制 5MB，并对药物/单药时间/三类生命体征/日志日期/单日服药核对、训练和游戏记录设上限；外部超限一律拒绝，不静默截断健康数据。② 恢复覆盖前先在 `localStorage` 写入一个本机恢复点；恢复点读写失败则不覆盖原数据，主数据写盘失败则同时回滚内存和旧恢复点。③ 设置页在恢复后显示一次性「撤销上次恢复」，撤销成功即删除恢复点；清空前先清理恢复点，清理失败则拒绝清空，避免旧健康数据残留可撤销。④ 目标值备份校验与设置页范围统一，反向高/低压回落默认。⑤ 文件选择器和 Store 共用同一大小常量；导出改用紧凑 JSON，避免缩进空白让本应用导出的临界备份又无法导入。新增单测覆盖恢复/撤销成功、两类写盘失败回滚、恢复点清理失败、超限预解析拒绝、各容量上限和清空清理；契约测试锁定撤销入口、文案与统一限制。 |
| 2026-08-10 | v0.2.19 | 生产响应头加固并上线：新增 Cloudflare Pages `_headers` 全站规则，线上响应增加 CSP、HSTS、防 iframe 嵌入、`nosniff`、无来源泄露策略和敏感浏览器能力禁用；CSP 继续允许现有行内样式，但脚本只准本站、禁止联网与对象/表单/框架。`deploy.sh` 明确把 `_headers` 放进直传目录；契约测试同时检查规则内容和部署包，避免“本地文件存在但上线漏带”。配置依据为 Cloudflare Pages Headers 官方文档（2026-04-21 更新）。本地 `wrangler pages dev` 确认规则解析并应用；生产部署 `26600867` 完成后，首页、重定向和 JS 均实测返回全部安全头，生产无头浏览器在新 CSP 下正常渲染且无拒绝/JS 异常。 |
| 2026-08-10 | v0.2.18 | 隐私边界加固：不对 `localStorage` 做无口令的伪加密，避免增加老人日常解锁负担与忘记口令导致的数据锁死；设置页改为明确说明同设备/同浏览器可见风险，下载明文 JSON 备份前增加一次敏感健康信息确认，提示仅保存到本人设备或可信位置、不要转发给无关人员。README 与技术决策同步纠正“本地存储零隐私风险”的错误表述，契约测试锁定提醒不得回退。现有备份格式和恢复兼容性不变。 |
| 2026-08-10 | v0.2.17 | 数据可靠性与安全护栏：① `storage.js` 增加统一深度规范化，加载/备份/恢复都会清洗非法日期、数字、枚举、ID、嵌套记录；拒绝未知备份 schema，避免畸形 JSON 进入渲染和统计。② 所有写操作返回成功状态，写盘失败回滚到最近成功快照；应用层保存失败保留表单并给出明确提示。③ 游戏成绩与训练打卡合并为一次事务，删除药物同步清理服药核对记录。④ 停药语义拆分为「撤销误停」与「登记新疗程」：新疗程用 `previousCourseId` 关联，停药空档不计应服，旧疗程已有后续时禁止撤销。⑤ 增加 CSP（禁止联网/第三方脚本）、5MB 备份大小限制、动态属性转义与 48px 小按钮下限。⑥ 新增 `test/contracts.test.js`，smoke 增加 file:// 直开、网络/浏览器硬超时和临时目录清理。验证：storage/contracts/figures 单测、全部 JS 语法检查、`bash test/smoke.sh` 全部通过；storage 单测中的 QuotaExceededError 与损坏 JSON 告警均为预期输出。 |
| 2026-08-10 | v0.2.16.1 | 上线落档：v0.2.12～v0.2.16 一并部署到 Cloudflare Pages（`./deploy.sh`），线上 12 个文件与本地逐字节一致（12/12 OK）。无代码改动。 |
| 2026-08-08 | v0.2.16 | 交互打磨（用户："应用整体的交互再优化一下，对标目前做的比较好的应用"）。① **安卓实体返回键先关浮层、不再直接退出应用**——每开一个浮层（弹窗/训练引导页）就 `pushState` 压一个历史条目，返回键消耗它后在 `popstate` 里关最上层；屏幕上的 ✕ 关闭时 DOM 立即移除并记一笔"欠账"再 `history.back()`，`popstate` 见欠账只还账不再多关一层（否则一次关两层）。游戏里"换个不用算的"复用同一条目（返回一次即退出）；没有浮层时返回键正常离开，不把人困住。② **打卡后不再跳回页首**：`render(view, {keepScroll})`，训练打卡走 keepScroll——原先在训练页往下翻着练，练完一个就被弹回顶部。③ Esc 关浮层（家属用电脑帮着录数据）。④ 按下反馈：卡片/列表项按下变底色，按钮 `scale(0.97)`，`touch-action: manipulation` 去掉移动端约 300ms 点击延迟。⑤ 弹窗从下滑入 + 遮罩淡入，不再硬切。⑥ `:focus-visible` 键盘焦点环。⑦ **尊重 `prefers-reduced-motion`**：系统里关了动效就几乎不动（晕动/前庭不适的人需要）。触控目标尺寸与字号一律没动，适老化前提不变。⑧ 验证：两个临时探针共 37 项——含返回键关弹窗/关引导页且 URL 不变、连开两层弹窗（设置→指引）只关一层、反复开关 5 次历史不攒坏、换动作后返回一次即退出、无浮层时能真的离开、Esc、打卡前后 scrollY 不变（500→500）。过程中修掉一个真 bug：第一版没有压历史条目，返回键吃掉了真实页面条目导致**整页重载**、后续断言全崩；另有 4 项失败是探针自己基线不干净（首次打开自动弹指引多压了一条），非应用问题。 |
| 2026-08-08 | v0.2.15 | 用药记录支持**停药**（用户："有的药品是到某一阶段就没再吃的，这种如何记录合适"）。原先只有"删除"一条路：删了复诊时查不到吃过什么，不删则天天显示漏服、还把完成率算低。① 药物新增 `from`/`to` 字段，停药是**独立于删除的第三种操作**（`Store.stopMed/undoStopMed/restartMed`）：从每日核对里消失、不计入完成率，但记录保留、在「已停用的药」分区灰显并标注"X 至 Y 停用"；重新开药创建带 `previousCourseId` 的独立疗程，停药空档不计应服；② 所有计数改走 `Store.medsOn(date)`（`medCountOn/medProgressToday/adherence7d/medFullDays/medStatusOn` 全部收敛到它），**顺带修掉 §9 里记的已知取舍**——历史日期的分母不再被今天的改动带偏；③ 新登记的药默认 `from=今天`，不会把登记之前的日子冤枉成漏服；④ 导出报告单独列「已停用的药」小节并写明吃到哪天（复诊时医生常问）；⑤ 编辑弹窗里把「吃到这天为止」和「删除（登记错了才用）」明确分开并说明区别，删除二次确认里点明"删除会连历史一起消失"；⑥ 缺 `from`/`to` 的旧数据视为"一直在吃"，行为不变；⑦ storage 单测新增 20+ 条断言（停药当天仍算在吃、次日起不算、停药不删记录、报告小节、恢复服用、旧数据兼容）。双抗吃满约 21 天改单药这类情况现在记得下来了。另：今日页「康复训练」那行补上阶段名（原先只写"6 项"，四个阶段恰好都是 6 项，看不出联动——经核对联动本身正常，是显示信息不足）。 |
| 2026-08-08 | v0.2.14 | 简笔画从"手填坐标"改为**画法体系**（用户质疑"网上没有相关资源吗？试着学习这些技能"——之前把"需要医生确认"当成了不做功课的理由）。① 查了人体比例标准（成人 7.5 头身、肩宽≈2 头，来源见 RESEARCH.md §十）与踝泵的通用动作描述，据此重写 `js/figures.js` 为三层：`FIG`（骨架 `SEG` + `at()` 按角度走关节 + `joint()` **两连杆逆解**由髋与脚反求膝 + `figure()` 渲染 + `foot()` 楔形脚）、`POSES`（姿势全部**算**出来，肢段长度由构造保证一致）、`FIGURES`（加载时渲染，app.js 接口不变）。② 新增 `test/figures.test.js`：把"画得对不对"变成几何断言——肢段长度符合骨架、**两帧必须是同一个人**（同名肢段等长）、不穿地、两帧差异足够大、key 对得上真实动作 id，外加每个动作的医学要点断言。**它第一次运行就抓出真 bug**（踏步站立帧远侧腿画到了髋以上）。③ 动作从 3 个增加到 **5 个**（补 `sit-to-stand` 坐站转移、`march` 原地踏步）。④ 靠放大人眼检查修掉一批只有看图才发现的错误：整条腿悬空在床面上方 12 单位、跖屈的脚穿进床垫里（改为脚尖与小腿大致共线）、严格侧面直上举时手与头挤成"两个圆圈"（改为符合 Bobath 做法的**前屈上举**并加断言要求手与头横向拉开 ≥8）、脚画成线几乎看不见（改楔形）、虚线残影放大后是一团碎线（删除）、颈部连到头圆心在头里留一根杆（改为画到圆周为止）。⑤ 探针验证 5 个动作在 **360px 小屏 + 特大字号**下均正常渲染不溢出、没有图的动作仍退回大 emoji、0 JS 报错（探针已删）。⑥ CLAUDE.md/DEVELOPMENT.md 常用命令补 `node test/figures.test.js`；RESEARCH.md 新增 §十 记录画法依据与"仍需专业复核"的边界。**姿势正确性仍需康复医生复核**，尤其患侧摆位与关节角度。 |
| 2026-08-08 | v0.2.13 | 落地路线图第 2、7 项：**语音朗读 + 动作示意简笔画**。① 新增 `js/speech.js`（`Speech` 全局）：Web Speech API 封装，中文音色优选、按句切分排队（防长文本截断）、`onend` 不触发时用看门狗兜底、**1.5 秒起播看门狗**识别"API 齐全但没有语音包"的静默失败环境（部分微信 WebView）并回调 `onFail`。② 训练引导页加「🔊 听一遍动作要领」（念名称→目的→分步→建议量→注意），科普文章弹窗加「🔊 听这篇」（剥 HTML 标签）；关引导页/关弹窗/切页一律 `Speech.stop()`（否则 iOS 继续念）。③ 设置页新增三档语速（慢/适中/快，默认**慢**——老人听得清比听得快重要），点档位即试听；不支持的浏览器不显示按钮、改在设置页说明原因与办法。`Store.profile.speechRate` 持久化并做枚举消毒（连 `font` 一并补上消毒）。④ 新增 `js/figures.js`（`FIGURES` 全局）：内联 SVG **两帧对照**简笔画（左=起始/右=动作到位），线条用 `currentColor` 跟随主题与字号、放大不失真；`figureHTML(ex)` 有图就画、没图退回大 emoji 不留空位；图下 `alt` 文字既给读屏也给看不清的人。**试点仅 3 个动作**（踝泵/Bobath握手/桥式），其余 25 个**故意留空等医生确认**——错的示意图比没有图更危险。⑤ 验证：storage 单测补 5 条枚举消毒断言（含旧备份缺字段回落）；临时探针用 stub 语音引擎跑通 33 项（朗读文稿内容/语速/中文音色/切分段数/停止/三种退出都停/文章去标签/设置持久化）并**专门测了"有 API 无音色"和"完全无 API"两档降级**；简笔画用无头截图**人眼检查三轮**（AI 写 SVG 坐标是盲画，必须看图）——第一轮 Bobath 举手与头顶同高像两个圆圈、踝泵像抽象折线，第二轮修好躯干与手臂，第三轮给踝泵加大脚楔形+对侧残影+旋转弧线才在 150px 小尺寸下看得出差异。测试中还发现并修掉：探针 stub 用 `window.speechSynthesis =` 赋值静默失败（该属性是只读访问器，必须 `defineProperty`），而这个失败恰好暴露了真实环境的"无音色静默失败"问题；另移除了一个语义不可靠的 `hasVoice()` 接口（`getVoices()` 为空时无法区分"未加载"与"没有"）。smoke.sh 静态资源清单补两个新文件。**语音与简笔画的医学正确性均需真机/医生确认**。 |
| 2026-08-08 | v0.2.12 | **少让患者算数 + 增强对患者的认同**（用户："脑梗患者算数有困难，不要让病人产生逆反心理，要增强对他们的认同"）。原则化为硬约定 #5，做法见 §6「少算数原则」「认同患者的措辞」「算一算」。要点：① 凡需心算比率处改为应用算好说结论（`leftText/dotsHTML/repTrackHTML/deltaLineHTML` 等），结论在前数字在后；② 用药页主指标从百分比改「7 天有 N 天全吃到」，删指责式表述；③ 「算一算」重做：答错不判错不记分、看提示/跳过不罚、结果无比分、留退路；④ 取消分数评语。医学事实与安全警示不删弱。测试：storage +5 断言 + 4 探针（穷举两位数加减校验拆解算式）。真机未测，见 MANUAL-TEST。 |
| 2026-08-04 | v0.2.11 | 记录页目标值与图表交互（用户反馈"基准线应可调 + 图表加交互"，方案按用户选定"判定跟个人目标走"）：① **个人目标值可调（遵医嘱）**——设置页新增「个人目标值」卡（血压高压/低压、血糖空腹/餐后2h，默认 140/90、7/10），存 `Store.profile.targets`，旧数据/旧备份自动回落默认并做数字消毒（`sanitizeTargets`）；② **血压/血糖判定改用个人目标**：「偏高（超过您的目标值）」/「在您的目标值内（遵医嘱）」，绝对安全线（血压 ≥180/110、<90/60，血糖 ≤3.9、明显偏高联系医生档）保持硬编码不随目标变；③ **血压图目标区着色**：低压~高压间浅绿目标区（zones 着色），目标与默认 140/90 不同时补淡色 140/90 参考线，图例显示「目标区（遵医嘱）」与当前目标值；④ **图表交互**：点按/悬停选中最近数据列（容错 32px），高亮选中点 + 其余压暗 45% + 垂直引导线，DOM tooltip 显示日期/时间/数值/判定（防边缘裁剪、关键信息仍在下方列表，符合无障碍惯例）；⑤ charts.js 支持 `zones` 着色与 `onTap/onLeave` 回调，每次 `Charts.line` 替换旧事件防重复绑定；⑥ 测试：storage.test.js 补目标值默认与消毒断言；带数据探针核验目标区图例、tooltip 弹层、设置页目标输入。真机未测，随历史视图一并手测。 |
| 2026-08-03 | v0.2.10 | 记录页可视化优化（用户反馈：图例颜色/文字与折线不对应）：① app.js 新增 `VITAL_SERIES` 单一数据源（系列/参考线颜色集中定义），canvas 画线与图例同源取色，杜绝颜色漂移；② 图例由纯文本改为**彩色线样例**（实线=数据、同色虚线=参考线，标签深色保对比度），血压图例含「140/90 提示线」、血糖含「空腹参考 7.0 / 餐后2小时参考 10.0」+「目标遵医嘱」注，体重图例为「体重」；③ 调色统一：血糖餐后参考线由与体重撞色的紫色改青色 #0E7490；④ 色弱双保险：血压低压点改方形标记、高压保持圆点；⑤ 血压参考线补图内 "140"/"90" 标注（血糖此前已有）；⑥ storage.js `vitalsSorted` 比较器补全三态，同时间记录按录入先后稳定排序（后录入视为最新），「最近记录」卡取值可预期；⑦ storage.test.js 补同时间排序断言。历史列表状态点（红黄绿=健康状态）语义保持不变。css 新增 .chart-legend/.lg-* 样式。验证：storage 单测（含同时间排序断言）全过、五页冒烟全过、带数据临时探针页无头打开记录页/血压历史弹窗核验三种指标图例渲染、0 JS 报错（探针已删除）。无新增提交文件、无新增全局对象。真机未测，随 v0.2.9 历史视图一并手测。 |
| 2026-08-03 | v0.2.9.1 | 运维：`deploy.sh` 不再硬依赖全局 wrangler（找不到则回落 `npx wrangler@latest`），并在部署前检查登录状态、未登录时给出明确指引后退出。起因：本机 wrangler 未全局安装且 `wrangler whoami` 报未登录，v0.2.9 因此**只推送了代码、未上线**（HANDOVER §1.2 已注明）。**当晚已上线**：用户在本机终端确认登录态正常（demoqqxu@gmail.com）后 `./deploy.sh` 部署成功，线上 JS 文件与本地逐字节一致（6/6）；`deploy.sh` 另补 `chmod +x` 可直接 `./` 运行（仅 mode 变更，无代码改动）。无应用代码改动。 |
| 2026-08-03 | v0.2.9 | 补齐历史视图（落地 §9 两项待决策），并按用户要求"血压之类做成可视化"：① **记录页历史前置**——移除输入表单下方的"历史记录（近20条）"卡，改为「最近血压/血糖/体重」卡内的 `📄 查看全部历史记录（N 条）` 按钮，弹窗含**近 30 条趋势图 + 全部记录**（不再截断 20 条），每条带**红/黄/绿状态点**与判定文字（血压/血糖沿用既有阈值文案，体重按 BMI，未填身高则不判定），删除操作移入弹窗。② **训练历史**——训练页新增「📅 训练打卡记录」卡：累计/连续天数 + **近 4 周打卡日历**（按周几对齐、颜色深浅表示当天项数、今天描边），`查看每天练了什么` 弹窗按日期列出动作名与**游戏成绩**（游戏历史一并解决）。③ **服药历史**——依从率卡新增 `查看服药历史（近14天）`，弹窗为近 14 天完成率 + 逐日**圆点图**（一点=一次应服，实心绿=已核对）；注明应服次数按当前药物清单计算、用药调整遵医嘱。④ storage.js 新增 `recentDates/exercisesOn/gamesOn/exerciseDaysTotal/activeDates/exerciseCalendar/medStatusOn/medHistory`，`exportReport` 改用 `exerciseDaysTotal()` 去重；app.js 抽出 `drawVitalChart()` 供记录页与弹窗共用（原三处重复的 Charts.line 调用合并）。⑤ 测试：storage.test.js 新增 14 条历史 API 断言（含"只有游戏成绩的日期也算活跃日""游戏不计入训练打卡天数"）；smoke.sh 训练页加断言「训练打卡记录」；另用带数据的临时探针页无头验证三个弹窗渲染正确、0 JS 报错（探针已删除）。无新增文件、无新增全局对象。 |
| 2026-08-02 | v0.2.8 | 文档落档（无代码改动）：① 用户手机实测（录屏，经 OCR 复核）确认首次指引、设置含备份按钮、保存血压 135/85、知识页、导出报告均正常；tel:120 与 360px 小屏仍未验证（HANDOVER §1.1/§1.2 同步）。② §9 记录两个局限：训练/用药/游戏历史无应用内视图（缺口非设计）；记录页历史在表单下方、小屏易漏（是否前置待定）。③ §9 路线图第 3 项"数据导入"标记已完成。 |
| 2026-08-02 | v0.2.7 | 文档：Cloudflare 部署账号归属已由用户确认为本人；§7 手工回归清单补「首次指引」「备份/恢复」两项。无代码改动 |
| 2026-08-02 | v0.2.6 | 导出/持久化做扎实（用户要求）：① 新增**全量备份导出**（设置页"备份全部数据"，下载带 app 标识/schema 版本/导出时间的 JSON 文件）；② 新增**恢复导入**（"从备份恢复"：选文件→校验文件身份→预览恢复将包含的药物/血压/血糖/体重/打卡条数→确认覆盖后整体恢复）；③ **字段级类型守卫**：非本应用备份一律拒绝、畸形内容按字段回落默认，防止误选任意文件清空数据；④ storage.test.js 新增备份往返/3 种畸形拒绝/部分字段合并用例，全部通过。换机迁移问题（路线图第 6 项）就此落地；⑤ HANDOVER §1.2/§6 同步（备份/恢复已完成、路线图状态更新）。 |
| 2026-08-02 | v0.2.5 | ① 新增**首次使用指引**：首次打开自动弹窗引导（测血压→服药→训练→记录/急救），"我知道了"后持久化（`Store.ui.guideSeen`），设置页新增"查看使用指引"可随时重看；指引文案只复述既有已审核内容，未引入新医学论断。② **医学内容复核**（用户最高优先项"不给错误消息"）：逐条核对 RESEARCH §八 全部易错点确认已落实；抓出 7 处遗留问题（0 高危/1 中/6 低）**全部修正**——含钾代盐禁忌补全"普利/沙坦类降压药或保钾利尿剂"（高钾血症风险，中危）；"复发和死亡风险≈1/3"按调研原文改为"再次中风风险≈1/3"（两处，CNSR-II 复发 HR0.37/死亡 HR0.20 分开表述）；双抗"约3周"补"多见于轻型/小卒中"限定（CHANCE/POINT 证据）；限盐 5g 补"遵医嘱"措辞（落实 §8.3 守则）；低血糖警示线改 `<=3.9`（ADA 警戒线 ≤3.9）；血压 ok 档提示语补"目标遵医嘱"避免"已达标"误读；血糖图增加餐后 10.0 参考线与图注（消除餐后值误读）。③ HANDOVER 记录部署账号归属（demoqqxu@gmail.com，待用户确认本人账号）。 |
| 2026-08-01 | v0.2.4 | 部署：仓库转公开；Cloudflare Pages 项目 stroke-rehab-assistant 上线（https://stroke-rehab-assistant.pages.dev/），直传 10 个静态文件；新增 deploy.sh 一键部署脚本；线上验证（内容一致性 + 五页冒烟）全过。无应用代码改动 |
| 2026-08-01 | v0.2.3 | 启用版本管理（经用户确认）：项目内 `git init`（main 分支），首次提交 3bf06fb 为 v0.2.2 全量状态，推送至私有仓库 github.com/wowayou/stroke-rehab-assistant；HANDOVER §1.2/§4/§6 相应条目同步更新。无代码改动 |
| 2026-08-01 | v0.2.2 | 接手核验后文档纠错：README 科普篇数 8→9（v0.2 新增《良肢位摆放》后未同步）；HANDOVER 动作数"31 个分期动作"→"28 个训练动作"（实际 28 动作 + 4 游戏共 32 项，其中 14 个带分期）；HANDOVER §3 补记"上级空 .git 会让 AI 会话环境快照误报 git 仓库"。无代码改动，单测/冒烟/语法检查在本次核验中全部通过 |
| 2026-08-01 | v0.2.1 | 移交准备：新增 docs/HANDOVER.md（交接状态/环境备忘/红线）与 AGENTS.md（工具无关的硬约定，供非 Claude 的 AI 工具读取）；README/CLAUDE.md 增加指引。无代码改动 |
| 2026-08-01 | v0.2 | 按调研报告（docs/RESEARCH.md）修订医学内容：血压目标改为"耐受时<130/80"主线+例外分支表述；HbA1c 改为个体化参考值+低血糖警示；新增依从性数据（复发风险≈1/3）与双抗21天提醒；运动量对齐指南表述+有氧运动评估门槛；新增含钾代盐、久坐提醒、六字速记；新增《良肢位摆放》文章；强化 Bobath 握手肩保护警示 |
| 2026-08-01 | v0.1 | 初版：五页框架、30+ 训练动作、4 个认知游戏、血压/血糖/体重记录与图表、用药核对与依从率、8 篇科普、BE-FAST 急救、适老化三档字号、单元测试与冒烟测试脚本 |
